<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boardspring.example.mapper.BoardMapper">
	<select id="selectBoardList" resultType="Board">
		SELECT	bNum,
				bId,
				bPw,
				bTitle,
				bContent,
				bUser,
				bDate,
				bHit
		FROM	board 
	</select>
	<insert id="boardInsert" parameterType="com.boardspring.example.domain.Board">
		INSERT INTO board (bNum, bTitle, bContent, bId, bDate)
			values
		(#{bNum},#{title},#{content},#{id},#{bDate},0)
	</insert>
</mapper>